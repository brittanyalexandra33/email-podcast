# TTS stub. Replace with ElevenLabs/Azure/Polly/Coqui as you prefer.
from pathlib import Path

def synthesize_to_mp3(text: str, out_path: Path):
    # Write placeholder bytes so the pipeline completes during setup.
    out_path.write_bytes(b'FAKE_MP3_BYTES')
